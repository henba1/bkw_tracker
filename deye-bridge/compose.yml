# publish:exclude — legacy stub; not shipped. Use root compose.yml via ./setup.sh. See PUBLISH.md.
# Legacy per-service compose. Prefer the root stack:
#   ./scripts/stack.sh up deye-bridge
#
# Or manually:
#   docker compose --env-file ../stack.env -f ../compose.yml up -d deye-bridge
